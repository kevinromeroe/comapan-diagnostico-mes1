# Pipeline runtime
pyyaml>=6.0.1
requests>=2.32.0
python-dateutil>=2.9.0
Pillow>=10.4.0
anthropic>=0.39.0

# Apify (oficial client)
apify-client>=1.8.0

# GA4 (opcional, solo si enabled)
google-analytics-data>=0.18.0
google-auth>=2.34.0

# Notificaciones
# (SMTP usa la stdlib, no requiere paquete)

# Dev
pytest>=8.3.0
pytest-cov>=5.0.0
ruff>=0.7.0
mypy>=1.13.0
